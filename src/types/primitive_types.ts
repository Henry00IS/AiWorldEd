/**
 * Enumeration of supported primitive mesh types.
 * Used by the primitive creation tool and toolbar buttons.
 */
export enum PrimitiveType {
  CUBE = 'cube',
  SPHERE = 'sphere',
  CYLINDER = 'cylinder',
  PLANE = 'plane'
}
