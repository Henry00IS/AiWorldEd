/**
 * The current selection mode of the editor.
 * OBJECT mode selects whole meshes, FACE mode selects individual triangle faces.
 */
export enum SelectionMode {
  OBJECT = 'Object',
  FACE = 'Face'
}
