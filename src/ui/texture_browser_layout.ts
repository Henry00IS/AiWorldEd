/**
 * Gap between texture browser tiles in pixels (matches stylesheet gap).
 */
export const TEXTURE_BROWSER_TILE_GAP_PX = 8;

/**
 * Minimum thumbnail track size in pixels (matches stylesheet minmax).
 */
export const TEXTURE_BROWSER_MIN_THUMB_PX = 96;
